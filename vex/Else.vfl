




#include <attrib.vfl>

vector pos[] = point(0, "P", primpoints(0, @primnum));

addpoint(0, avg(pos));
addpoint(0, (pos[0] + pos[1]) / 2);
addpoint(0, (pos[1] + pos[2]) / 2);
addpoint(0, (pos[2] + pos[0]) / 2);































